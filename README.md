
  # Disaster Response Web App UI

  This is a code bundle for Disaster Response Web App UI. The original project is available at https://www.figma.com/design/by0UO8jJ7gUHSBByMxXmvt/Disaster-Response-Web-App-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  